import { Text, View } from 'react-native';

const Last14DaysExpenseScreen = () => {
	return (
		<View>
			<Text>This is the last 14 days expense screen</Text>
		</View>
	);
};

export default Last14DaysExpenseScreen;
